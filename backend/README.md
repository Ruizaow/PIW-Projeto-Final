Para rodar o servidor, instale no terminal da pasta:
```
npm init -y
npm install -d typescript @types/node
npx tsc --init

npm install -d nodemon ts-node

npm install express
npm install -D @types/node @types/express

npm install express-handlebars
```
